spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sourcelens?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: sourcelens
    password: sourcelens123

  data:
    redis:
      host: localhost
      port: 6379

# flyway 已在 application.yml 中配置，这里仅保留测试配置覆盖
# flyway:
#   locations: classpath:db/migration
